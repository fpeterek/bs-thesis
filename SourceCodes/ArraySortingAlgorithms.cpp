#include <iostream>

int main(int argc, const char * argv[]) {
    std::cout << "Rewritten so as to avoid people accusing me of distributing their " <<
        << "publicly available source code." << std::endl;
    std::cout << "This code won't make it the final version, anyway." << std::endl;
}

